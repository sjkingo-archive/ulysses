(mostly) in descending order of urgency:

- DESIGN
- GCC cross-compiler (crossdev -p -s1 --binutils 2.18 --gcc 4.2.4 --target i386-pc-elf-gnu)
- Load kernel into higher half (0xC0000000)
- Protected mode!
- Exception handler (consider minix/kernel/exception.c ?)
- Detect upper memory from BIOS (http://wiki.osdev.org/How_Do_I_Determine_The_Amount_Of_RAM)
- Memory paging (http://wiki.osdev.org/Setting_Up_Paging)
- PQ implementation (doubly LL)
- Process table (actually add process memory maps)
- Scheduler (round-robin)
- finish kprintf
